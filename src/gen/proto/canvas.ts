// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: proto/canvas.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "canvas";

export interface GenerateRequest {
  prompt: string;
  iterations: number;
  style: string;
}

export interface GenerateResponse {
  designId: string;
  status: string;
  designs: Design[];
}

export interface RefineRequest {
  designId: string;
  feedback: string;
}

export interface RefineResponse {
  designId: string;
  status: string;
}

/** Empty for now, can add filters later */
export interface ListRequest {}

export interface ListResponse {
  designs: Design[];
}

export interface SelectRequest {
  designId: string;
}

export interface SelectResponse {
  success: boolean;
}

export interface Design {
  id: string;
  prompt: string;
  htmlContent: string;
  createdAt: number;
}

function createBaseGenerateRequest(): GenerateRequest {
  return { prompt: "", iterations: 0, style: "" };
}

export const GenerateRequest: MessageFns<GenerateRequest> = {
  encode(message: GenerateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prompt !== "") {
      writer.uint32(10).string(message.prompt);
    }
    if (message.iterations !== 0) {
      writer.uint32(16).int32(message.iterations);
    }
    if (message.style !== "") {
      writer.uint32(26).string(message.style);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.iterations = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.style = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateRequest {
    return {
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      iterations: isSet(object.iterations) ? globalThis.Number(object.iterations) : 0,
      style: isSet(object.style) ? globalThis.String(object.style) : "",
    };
  },

  toJSON(message: GenerateRequest): unknown {
    const obj: any = {};
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.iterations !== 0) {
      obj.iterations = Math.round(message.iterations);
    }
    if (message.style !== "") {
      obj.style = message.style;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateRequest>, I>>(base?: I): GenerateRequest {
    return GenerateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateRequest>, I>>(object: I): GenerateRequest {
    const message = createBaseGenerateRequest();
    message.prompt = object.prompt ?? "";
    message.iterations = object.iterations ?? 0;
    message.style = object.style ?? "";
    return message;
  },
};

function createBaseGenerateResponse(): GenerateResponse {
  return { designId: "", status: "", designs: [] };
}

export const GenerateResponse: MessageFns<GenerateResponse> = {
  encode(message: GenerateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.designId !== "") {
      writer.uint32(10).string(message.designId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    for (const v of message.designs) {
      Design.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.designId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.designs.push(Design.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateResponse {
    return {
      designId: isSet(object.designId)
        ? globalThis.String(object.designId)
        : isSet(object.design_id)
          ? globalThis.String(object.design_id)
          : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      designs: globalThis.Array.isArray(object?.designs)
        ? object.designs.map((e: any) => Design.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenerateResponse): unknown {
    const obj: any = {};
    if (message.designId !== "") {
      obj.designId = message.designId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.designs?.length) {
      obj.designs = message.designs.map((e) => Design.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateResponse>, I>>(base?: I): GenerateResponse {
    return GenerateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateResponse>, I>>(object: I): GenerateResponse {
    const message = createBaseGenerateResponse();
    message.designId = object.designId ?? "";
    message.status = object.status ?? "";
    message.designs = object.designs?.map((e) => Design.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRefineRequest(): RefineRequest {
  return { designId: "", feedback: "" };
}

export const RefineRequest: MessageFns<RefineRequest> = {
  encode(message: RefineRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.designId !== "") {
      writer.uint32(10).string(message.designId);
    }
    if (message.feedback !== "") {
      writer.uint32(18).string(message.feedback);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefineRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefineRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.designId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.feedback = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefineRequest {
    return {
      designId: isSet(object.designId)
        ? globalThis.String(object.designId)
        : isSet(object.design_id)
          ? globalThis.String(object.design_id)
          : "",
      feedback: isSet(object.feedback) ? globalThis.String(object.feedback) : "",
    };
  },

  toJSON(message: RefineRequest): unknown {
    const obj: any = {};
    if (message.designId !== "") {
      obj.designId = message.designId;
    }
    if (message.feedback !== "") {
      obj.feedback = message.feedback;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefineRequest>, I>>(base?: I): RefineRequest {
    return RefineRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefineRequest>, I>>(object: I): RefineRequest {
    const message = createBaseRefineRequest();
    message.designId = object.designId ?? "";
    message.feedback = object.feedback ?? "";
    return message;
  },
};

function createBaseRefineResponse(): RefineResponse {
  return { designId: "", status: "" };
}

export const RefineResponse: MessageFns<RefineResponse> = {
  encode(message: RefineResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.designId !== "") {
      writer.uint32(10).string(message.designId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefineResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefineResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.designId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefineResponse {
    return {
      designId: isSet(object.designId)
        ? globalThis.String(object.designId)
        : isSet(object.design_id)
          ? globalThis.String(object.design_id)
          : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: RefineResponse): unknown {
    const obj: any = {};
    if (message.designId !== "") {
      obj.designId = message.designId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefineResponse>, I>>(base?: I): RefineResponse {
    return RefineResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefineResponse>, I>>(object: I): RefineResponse {
    const message = createBaseRefineResponse();
    message.designId = object.designId ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseListRequest(): ListRequest {
  return {};
}

export const ListRequest: MessageFns<ListRequest> = {
  encode(_: ListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListRequest {
    return {};
  },

  toJSON(_: ListRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRequest>, I>>(base?: I): ListRequest {
    return ListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRequest>, I>>(_: I): ListRequest {
    const message = createBaseListRequest();
    return message;
  },
};

function createBaseListResponse(): ListResponse {
  return { designs: [] };
}

export const ListResponse: MessageFns<ListResponse> = {
  encode(message: ListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.designs) {
      Design.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.designs.push(Design.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListResponse {
    return {
      designs: globalThis.Array.isArray(object?.designs)
        ? object.designs.map((e: any) => Design.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListResponse): unknown {
    const obj: any = {};
    if (message.designs?.length) {
      obj.designs = message.designs.map((e) => Design.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListResponse>, I>>(base?: I): ListResponse {
    return ListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListResponse>, I>>(object: I): ListResponse {
    const message = createBaseListResponse();
    message.designs = object.designs?.map((e) => Design.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSelectRequest(): SelectRequest {
  return { designId: "" };
}

export const SelectRequest: MessageFns<SelectRequest> = {
  encode(message: SelectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.designId !== "") {
      writer.uint32(10).string(message.designId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SelectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSelectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.designId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SelectRequest {
    return {
      designId: isSet(object.designId)
        ? globalThis.String(object.designId)
        : isSet(object.design_id)
          ? globalThis.String(object.design_id)
          : "",
    };
  },

  toJSON(message: SelectRequest): unknown {
    const obj: any = {};
    if (message.designId !== "") {
      obj.designId = message.designId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SelectRequest>, I>>(base?: I): SelectRequest {
    return SelectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SelectRequest>, I>>(object: I): SelectRequest {
    const message = createBaseSelectRequest();
    message.designId = object.designId ?? "";
    return message;
  },
};

function createBaseSelectResponse(): SelectResponse {
  return { success: false };
}

export const SelectResponse: MessageFns<SelectResponse> = {
  encode(message: SelectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SelectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSelectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SelectResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: SelectResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SelectResponse>, I>>(base?: I): SelectResponse {
    return SelectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SelectResponse>, I>>(object: I): SelectResponse {
    const message = createBaseSelectResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseDesign(): Design {
  return { id: "", prompt: "", htmlContent: "", createdAt: 0 };
}

export const Design: MessageFns<Design> = {
  encode(message: Design, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.prompt !== "") {
      writer.uint32(18).string(message.prompt);
    }
    if (message.htmlContent !== "") {
      writer.uint32(26).string(message.htmlContent);
    }
    if (message.createdAt !== 0) {
      writer.uint32(32).int64(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Design {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDesign();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.htmlContent = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Design {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      htmlContent: isSet(object.htmlContent)
        ? globalThis.String(object.htmlContent)
        : isSet(object.html_content)
          ? globalThis.String(object.html_content)
          : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.Number(object.createdAt)
        : isSet(object.created_at)
          ? globalThis.Number(object.created_at)
          : 0,
    };
  },

  toJSON(message: Design): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.htmlContent !== "") {
      obj.htmlContent = message.htmlContent;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Design>, I>>(base?: I): Design {
    return Design.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Design>, I>>(object: I): Design {
    const message = createBaseDesign();
    message.id = object.id ?? "";
    message.prompt = object.prompt ?? "";
    message.htmlContent = object.htmlContent ?? "";
    message.createdAt = object.createdAt ?? 0;
    return message;
  },
};

export interface CanvasServiceImplementation<CallContextExt = {}> {
  generate(
    request: GenerateRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GenerateResponse>>;
  refine(
    request: RefineRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RefineResponse>>;
  list(
    request: ListRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListResponse>>;
  select(
    request: SelectRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SelectResponse>>;
}

export interface CanvasServiceClient<CallOptionsExt = {}> {
  generate(
    request: DeepPartial<GenerateRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GenerateResponse>;
  refine(
    request: DeepPartial<RefineRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RefineResponse>;
  list(
    request: DeepPartial<ListRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListResponse>;
  select(
    request: DeepPartial<SelectRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SelectResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
